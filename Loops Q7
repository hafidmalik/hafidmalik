# include <iostream>
using namespace std;
int main(){
	int rows = 0;
	cout << "Enter # no Rows : ";
	cin >> rows;
	
	for( int i=0; i <= rows; i++ ){
		for( int _=0; _ < i; _++ ){
			cout << "X";
		}
		cout << "\n";
	}
	return 0;
}
